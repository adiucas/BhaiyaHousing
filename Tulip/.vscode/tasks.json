{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run SASS",
            "type": "shell",
            "command": "sass --watch scss:css --sourcemap=none --no-cache --style=expanded",
            "windows": {
                "command": "sass --watch scss:css --sourcemap=none --no-cache --style=expanded"
            },
            "presentation": {
                "reveal": "always",
                "panel": "new"
            },
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}